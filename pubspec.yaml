name: analog_clock_test
description: A new Flutter project for technical test for Bibit.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  charts_flutter_new: ^0.12.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_local_notifications: ^12.0.4
  freezed_annotation: ^2.2.0
  get_it: ^7.2.0
  injectable: ^2.0.0
  intl: ^0.17.0
  json_serializable: ^6.5.4
  rxdart: ^0.27.5
  timezone: ^0.9.0

dev_dependencies:
  build_runner: ^2.3.2
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.2.1
  injectable_generator: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - "assets/"
